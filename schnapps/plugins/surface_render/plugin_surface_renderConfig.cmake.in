@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
find_dependency(schnapps_core REQUIRED)
find_dependency(plugin_cmap_provider REQUIRED)

if(NOT TARGET schnapps::surface_render)
	include("${CMAKE_CURRENT_LIST_DIR}/plugin_surface_renderTargets.cmake")
endif()
