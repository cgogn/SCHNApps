@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
find_dependency(schnapps_core REQUIRED)
find_dependency(plugin_cmap_provider REQUIRED)

if(NOT TARGET schnapps::image)
	include("${CMAKE_CURRENT_LIST_DIR}/plugin_imageTargets.cmake")
endif()
