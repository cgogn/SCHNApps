@PACKAGE_INIT@
include(CMakeFindDependencyMacro)
find_dependency(schnapps_core REQUIRED)

if(NOT TARGET schnapps::selection)
	include("${CMAKE_CURRENT_LIST_DIR}/plugin_selectionTargets.cmake")
endif()
